import type { Case, Equipment } from './types';

// This is mock data. In a real application, this would come from a database.
// The engineers list is now managed in Firestore via the /engineers page.

export let allCases: Case[] = [];

export let equipment: Equipment[] = [];
